import { ViewLayout } from '../components';

const SettingsView = () => {
  return (
    <ViewLayout id="settings-view" type="tabs" title="Settings page title">
      ...SettingsView...
    </ViewLayout>
  );
};

export default SettingsView;
