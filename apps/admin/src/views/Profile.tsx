import { ViewLayout } from '../components';

const ProfileView = () => {
  return (
    <ViewLayout id="profile-view" type="profile" title="Profile page title">
      ...ProfileView...
    </ViewLayout>
  );
};

export default ProfileView;
