import { ViewLayout } from '../components';
import { Dashboard } from '../modules';

const DashboardView = () => <ViewLayout id="dashboard-view" children={<Dashboard />} />;

export default DashboardView;
