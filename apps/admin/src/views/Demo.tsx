import { ViewLayout } from '../components';
import { DemoExamples } from '../modules';

const DemoView = () => {
  return (
    <ViewLayout title="Demo">
      <DemoExamples />
    </ViewLayout>
  );
};

export default DemoView;
